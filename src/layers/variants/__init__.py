"""
Layer Variants and Alternative Implementations

This module provides alternative implementations and optimizations for each layer:
- Multiple L1-L4 variants with different trade-offs
- Framework-based implementations (streaming, recursive, etc.)
- GPU-accelerated variants where applicable
- Legacy implementations for backward compatibility

These are experimental or specialized implementations. Use main layer
implementations in core/ and advanced/ for production use.
"""

# Import variants for convenience
# Note: Specific variants should be imported directly from their modules

__all__ = [
    # Layer variants are available but imported explicitly as needed
    # e.g., from src.layers.variants.l5_framework import ...
]
